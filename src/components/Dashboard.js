import SearchBar from "./SearchBar";
import TaskBoard from "./TaskBoard";
const Dashboard = () => {
  return (
    <div>
      <SearchBar />
      <TaskBoard />
    </div>
  );
};

export default Dashboard;
